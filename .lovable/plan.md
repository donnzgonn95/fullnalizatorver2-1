## Cel\nLive feed (Binance WS + CoinGecko fallback), wskaźniki live, detektory setupów (Elliott + BB bounce), zapis do Supabase, auto-weryfikacja TP/SL.\n\n## 1. Migracja DB\n\n```sql\nCREATE TABLE public.detected_setups (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id uuid NULL,\n  symbol text NOT NULL,\n  interval text NOT NULL,           -- M15|M30|M45|H1|H4\n  setup_type text NOT NULL,         -- elliott_wave|bb_bounce\n  wave_label text,\n  direction text NOT NULL,          -- long|short\n  entry_price numeric NOT NULL,\n  stop_loss numeric NOT NULL,\n  take_profit numeric NOT NULL,\n  signal_strength numeric NOT NULL DEFAULT 0,\n  detected_at timestamptz NOT NULL DEFAULT now(),\n  entry_time timestamptz NOT NULL,\n